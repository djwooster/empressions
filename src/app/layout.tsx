import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Empressions Studio — Fine Tattoo Art in Memphis",
  description:
    "Empressions Studio is a luxury tattoo studio in Memphis specializing in fine line, realism, and black & grey work by artist Mckenna Wilson.",
  openGraph: {
    title: "Empressions Studio",
    description: "Fine Line · Realism · Black & Grey — Memphis, TN",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable}`}>
      <body className="antialiased">{children}</body>
    </html>
  );
}
